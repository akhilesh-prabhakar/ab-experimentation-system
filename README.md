# ab-experimentation-system
Backend-controlled A/B experimentation system with deterministic variant assignment, persistent experiment configuration, Dockerized local setup, and HTTP API on port 8080.
